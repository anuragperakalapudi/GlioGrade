# Evaluation metrics for imbalanced multiclass classification.
# Includes: balanced accuracy, macro F1, Cohen's Kappa, per-class P/R/F1, AUC-ROC (one-vs-rest).
# Never use plain accuracy as primary metric on this dataset (80% class imbalance).
